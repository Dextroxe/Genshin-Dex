import { IPlayerInfo } from "../types/index";
import { EnkaProfile, PlayerInfo } from "./index";
export declare class FetchUserProfile {
    profile: boolean;
    accounts: {
        is_uid_public: boolean;
        player: PlayerInfo;
    }[];
    enkaProfile: EnkaProfile;
    constructor(language: string, data: {
        profile: boolean;
        hoyos: {
            is_uid_public: boolean;
            player_info: IPlayerInfo;
        }[];
        user: any;
        id: string;
    });
}
